import { SectionPlaceholder } from '../SectionPlaceholder'

/** Sección Usuarios del panel /admin. Gestión de usuarios y roles (caso de uso §2). */
export function UsersSection() {
  return (
    <SectionPlaceholder
      title="Usuarios"
      description="Alta, edición y activación de usuarios; asignación de roles."
      phase="Fase 1"
    />
  )
}
