feat(admin): Fase 2 — AgroUnidades, Sectores y Contactos

Módulo de Administración — sesión 9, 2026-05-19.
Caso de uso admin_control_usecases.md §4–§6.

Tipos y hooks:
- Tipos AgroSector, AgroUnit, Contact, ContactAssignmentList derivados
  del schema openapi-typescript.
- useAgroSectors / useAgroUnits / useContacts con TanStack Query v5.
  AgroUnit usa PatchedAgroUnit para el payload de PATCH.
  ContactAssignment delete recibe id entero (no UUID).

Diálogos:
- CreateSectorDialog: sector_name, scian_code, activity_name, description
  (textarea nativo — shadcn no incluye Textarea en este setup).
- CreateAgroUnitDialog: 14 campos; CountryCombobox con default México;
  useStates(iso2) para estados en cascada.
- CreateContactDialog: crea contacto y si recibe agroUnitId asigna
  automáticamente con createAssignment.

Panel AgroUnitPanel:
- Tab Detalle: modo vista + edición inline (RHF + zod + applyDrfErrors).
- Tab Contactos: lista ContactAssignment + eliminar + Agregar contacto.
- Gates: canEdit → SUPER_ADMIN; canManageContacts → SUPERVISOR.

AgroUnitsSection:
- Tab Unidades: tabla + click → panel. Botón "Nueva Unidad" → SUPER_ADMIN.
- Tab Sectores: tabla. Botón "Nuevo Sector" → SUPERVISOR.

Decisión de diseño: AgroUnit es catálogo global — crear sin
DataCentralAssignment la deja invisible al creador (ScopeFilterMixin).
Solo SuperAdmin crea/edita/elimina unidades.

Tests: 5 casos en AgroUnitsSection.test.tsx con vi.mock (MSW
descartado en capa de sección por conflictos de trailing-slash).
npx tsc --noEmit → 0 errores. npx vitest run → 85/85.
